---
export interface Props {
  metadata?: Record<string, unknown>;
  title?: string;
  description?: string;
  image?: string;
}

const props = Astro.props;
---

<slot name="before" />
<slot />
<slot name="after" />
