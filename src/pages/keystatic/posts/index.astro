---
export const prerender = false;
import AdminLayout from '~/layouts/AdminLayout.astro';
import { verifySessionToken } from '~/lib/auth';
import { readTokenFromCookies } from '~/lib/token-store';
import { GitHubClient } from '~/lib/github';
import { parseMarkdown } from '~/lib/markdown';

const session = Astro.cookies.get('ks-admin-session')?.value;
const verified = session ? verifySessionToken(session) : null;
if (!verified) return Astro.redirect('/login');

const token = readTokenFromCookies(Astro.cookies);
if (!token) return Astro.redirect('/keystatic/github-token');

interface Row {
  name: string;
  path: string;
  sha: string;
  size: number;
  title: string;
  date: string;
  category: string;
  draft: boolean;
}

let rows: Row[] = [];
let loadError: string | null = null;

try {
  const client = new GitHubClient(token);
  const files = await client.listFiles('src/content/news/');
  const mdFiles = files.filter((f) => /\.(md|mdx)$/i.test(f.name));
  const enriched = await Promise.all(
    mdFiles.map(async (f) => {
      try {
        const file = await client.readFile(f.path);
        const parsed = parseMarkdown(file.content);
        const fm = parsed.frontmatter as Record<string, unknown>;
        return {
          name: f.name,
          path: f.path,
          sha: f.sha,
          size: f.size,
          title: (fm.title as string) ?? f.name.replace(/\.(md|mdx)$/i, ''),
          date: typeof fm.publishDate === 'string' ? fm.publishDate : '',
          category: typeof fm.category === 'string' ? fm.category : '',
          draft: fm.draft === true,
        } satisfies Row;
      } catch {
        return {
          name: f.name,
          path: f.path,
          sha: f.sha,
          size: f.size,
          title: f.name.replace(/\.(md|mdx)$/i, ''),
          date: '',
          category: '',
          draft: false,
        } satisfies Row;
      }
    }),
  );
  rows = enriched.sort((a, b) => (b.date || '').localeCompare(a.date || ''));
} catch (err) {
  loadError = err instanceof Error ? err.message : 'Failed to load';
}
---
<AdminLayout title="Posts" active="posts" username={verified?.username}>
  <div class="flex items-center justify-between mb-6">
    <div>
      <h1 class="text-2xl font-semibold">Posts</h1>
      <p class="mt-1 text-sm text-gray-500">{rows.length} posts total</p>
    </div>
    <a
      href="/keystatic/posts/new"
      class="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-md"
    >+ New Post</a>
  </div>

  {loadError && (
    <div class="mb-4 rounded-md border border-red-200 bg-red-50 dark:bg-red-950/40 dark:border-red-900 p-3 text-sm text-red-700 dark:text-red-300">
      Failed to load: {loadError} — Please check if the token still has repository access
    </div>
  )}

  <div class="rounded-xl border border-gray-200 dark:border-gray-800 bg-white dark:bg-gray-950 overflow-hidden">
    <table class="w-full text-sm">
      <thead>
        <tr class="bg-gray-50 dark:bg-gray-900 text-left text-xs uppercase tracking-wide text-gray-500">
          <th class="px-4 py-3 font-medium">Title</th>
          <th class="px-4 py-3 font-medium w-32">Category</th>
          <th class="px-4 py-3 font-medium w-32">Date</th>
          <th class="px-4 py-3 font-medium w-20">Status</th>
          <th class="px-4 py-3 font-medium w-20 text-right">Actions</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-200 dark:divide-gray-800">
        {rows.length === 0 && !loadError && (
          <tr><td colspan="5" class="px-4 py-12 text-center text-gray-500">No posts yet. Click + New Post to create the first one.</td></tr>
        )}
        {rows.map((row) => (
          <tr class="hover:bg-gray-50 dark:hover:bg-gray-900/50">
            <td class="px-4 py-3">
              <a href={`/keystatic/posts/${encodeURIComponent(row.name)}`} class="text-blue-600 dark:text-blue-400 hover:underline font-medium">{row.title}</a>
              <div class="text-xs text-gray-400 font-mono mt-0.5">{row.name}</div>
            </td>
            <td class="px-4 py-3">
              {row.category ? (
                <span class="inline-block px-2 py-0.5 rounded-full bg-blue-50 dark:bg-blue-950 text-blue-700 dark:text-blue-300 text-xs">{row.category}</span>
              ) : <span class="text-gray-400">—</span>}
            </td>
            <td class="px-4 py-3 text-gray-600 dark:text-gray-300">{row.date || <span class="text-gray-400">—</span>}</td>
            <td class="px-4 py-3">
              {row.draft ? <span class="text-xs px-2 py-0.5 rounded bg-amber-100 dark:bg-amber-900/40 text-amber-700 dark:text-amber-300">Draft</span> : <span class="text-xs text-green-600 dark:text-green-400">Published</span>}
            </td>
            <td class="px-4 py-3 text-right">
              <button
                type="button"
                data-delete-post
                data-name={row.name}
                data-path={row.path}
                data-sha={row.sha}
                class="text-red-600 dark:text-red-400 hover:underline text-xs"
              >Delete</button>
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>

  <script is:inline>
    document.addEventListener('click', async (e) => {
      const target = e.target instanceof Element ? e.target.closest('[data-delete-post]') : null;
      if (!target) return;
      e.preventDefault();
      const { name, path, sha } = target.dataset;
      if (!confirm(`Are you sure you want to delete "${name}"?\nThis will be committed and pushed to the GitHub main branch.`)) return;
      const original = target.textContent;
      target.textContent = 'Deleting...';
      target.setAttribute('disabled', 'true');
      try {
        const res = await fetch('/api/admin/delete', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ path, sha, commitMessage: `Delete post: ${name}` }),
        });
        const data = await res.json();
        if (!res.ok || !data.success) {
          window.__adminToast?.(data.error || 'Delete failed', 'error');
          target.textContent = original;
          target.removeAttribute('disabled');
        } else {
          window.__adminToast?.('Deleted, refreshing list...', 'success');
          setTimeout(() => window.location.reload(), 600);
        }
      } catch {
        window.__adminToast?.('Network error', 'error');
        target.textContent = original;
        target.removeAttribute('disabled');
      }
    });
  </script>
</AdminLayout>
