---
import Layout from '~/layouts/Layout.astro';
import Footer from '~/components/widgets/Footer.astro';
import ChatWidget from '~/components/ChatWidget.astro';
import { footerData } from '~/navigation';
import { getHomePermalink } from '~/utils/permalinks';
import { SITE } from 'astrowind:config';

const title = `Page not found — ${SITE.name}`;
const description = 'Sorry, the page you are looking for has drifted out to sea. Use the links below to find your way back.';
---

<Layout
  metadata={{
    title,
    description,
    robots: { index: false, follow: false },
  }}
>
  <section class="flex items-center min-h-[60vh] p-8">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8 text-center">
      <div class="max-w-md">
        <h1 class="mb-4 font-black text-9xl">
          <span class="sr-only">Error</span>
          <span class="text-primary dark:text-blue-400">404</span>
        </h1>
        <p class="mb-2 text-2xl font-semibold md:text-3xl">Lost at sea.</p>
        <p class="mt-2 mb-8 text-lg text-muted dark:text-slate-400">{description}</p>
        <div class="flex flex-wrap justify-center gap-3">
          <a class="btn btn-primary" href={getHomePermalink()}>Back to homepage</a>
          <a class="btn" href="/products">Browse products</a>
          <a class="btn" href="/news">Read the news</a>
        </div>
      </div>
    </div>
  </section>
  <Footer {...footerData} />
  <ChatWidget />
</Layout>
